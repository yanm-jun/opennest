# OpenClaw Recipe

Runtime: `native-cli`

OpenNest should not copy or fork OpenClaw. It should install the CLI, manage token injection, start the gateway, and open the dashboard.

Recommended user flow:

```text
Install → Add Token → Start Gateway → Open Dashboard
```

Fallback:

```text
Run Official Onboarding → Start Gateway → Open Dashboard
```
