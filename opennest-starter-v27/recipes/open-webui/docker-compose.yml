services:
  open-webui:
    image: ghcr.io/open-webui/open-webui:main
    container_name: opennest-open-webui
    restart: unless-stopped
    ports:
      - "3000:8080"
    volumes:
      - open-webui-data:/app/backend/data

volumes:
  open-webui-data:
