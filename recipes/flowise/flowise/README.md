# Flowise Recipe

Runtime: `docker-compose`

This is a minimal starter compose for OpenNest runtime testing.

Flowise official Docker Compose flow usually starts from the official project docker folder and `.env.example`; once OpenNest runtime is stable, prefer syncing the official compose template for production.
