# Open WebUI Recipe

Runtime: `docker-compose`

This starter compose maps `127.0.0.1:3000` to the Open WebUI container port.

OpenNest controls:

```text
Install → docker compose up -d → Open Dashboard → Logs → Stop
```
